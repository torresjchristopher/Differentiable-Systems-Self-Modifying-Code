# Differentiable-Systems-Self-Modifying-Code
Differentiable Operating System where every line of code in the "kernel" is a parameter that can be optimized via a loss function. • The Tech to Build: A "Self-Refining Compiler." A system where the compiler doesn't just turn code into machine instructions, but uses a custom compiler to rewrite the code's logic to minimize execution time.
